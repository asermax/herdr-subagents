# esbuild ships a platform binary installed via a postinstall script. The build
# bundles the helper with esbuild, so the binary must be present. Allow the
# postinstall explicitly so `npm ci` produces a working build in CI and locally.
allow-scripts=esbuild
