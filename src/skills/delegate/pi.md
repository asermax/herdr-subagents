<!-- pi delegate skill fragment -->
<!-- Injects into {{wake}} in protocol.md -->

When a child finishes, your extension wakes you automatically. It spawns `{{helper}} watch` once per session and forwards each status change, so a terminal state becomes a wake with no action from you — you never arm the wake yourself.
